use crate::trie::Trie;

pub fn generate_puml(trie: &Trie, output_path: &str) {
    // À compléter
    let _ = trie;
    let _ = output_path;
    println!("generate_puml appelé (pas encore implémenté)");
}